@model IEnumerable<MVC_project.Models.DeliveryBoy>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeliveryBoyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeliveryBoyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeliveryBoyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeliveryBoyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeliveryBoyId })
        </td>
    </tr>
}

</table>
